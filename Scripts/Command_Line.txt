## Command Line Interface Commands

#pwd <- print working directory
#clear <- clear the command line
#ls <- list out all files/folders in current directory
#ls -a <- lists hidden and unhidden files and folders
#ls -al <- lists details for hidden and unhidden files and folders
#cd <- change working directory
#mkdir <- make a new folder
#touch <- make an empty file
#cp <- copy a file or folder (-r)
#rm <- remove a file or folder (-r)
#mv <- move a file or folder 
#date <- list the date
#echo <- print the arguments provided
